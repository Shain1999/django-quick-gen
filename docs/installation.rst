============
Installation
============

At the command line::

    $ easy_install django-quick-gen

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-quick-gen
    $ pip install django-quick-gen
